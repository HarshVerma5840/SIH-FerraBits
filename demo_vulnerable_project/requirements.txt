# Demo vulnerable Python project requirements
# Each version below is known to have at least one CVE in OSV / NVD

# CVE-2022-40897: Remote Code Execution via command injection
GitPython==3.1.29

# CVE-2023-32681: Session cookie leakage across redirect domains
requests==2.28.2

# CVE-2024-34064: HTML attribute injection via user-controlled keys
Jinja2==3.1.3

# Baseline safe version (no known CVEs at time of writing)
flask==3.0.0
